#!/bin/sh -x
 
# refresh repository
cd /var/lib/opdemand
git pull

# source orchestration inputs as environment vars
. /var/cache/opdemand/inputs.sh

# use relative path to find/exec script as root
sudo templates/$platform_path/$server_deploy_script 2>&1 | tee -a /var/log/opdemand/deploy.log